y : boolean = false;
z : boolean = true;

local x : integer = 5;
x1 : double = 10.5;
x3 : string = "fff";

if (y and z)
then
    add(a, v, d);
    add(d, r);
else
    true;
end;

add(a, v, d);

function getBiggest(t : table) : integer
    if (isBigger(x, y)) then
            return x;
    else
            return y;
    end;
end